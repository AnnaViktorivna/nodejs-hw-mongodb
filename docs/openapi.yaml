openapi: 3.1.0
info:
  version: 1.1.0
  title: <Contacts API>
  license:
    name: ApiTestLaptur 2.0
    url: <http://www.apache.org/licenses/LICENSE-2.0.html>
  description: >
 This is a documentation of contacts app
# tags:
# теги, що ви будете використовувати
servers:
  - url: <http://localhost:3000>
  # - url: #посилання на задеплоєний сайт
paths:
  # /contacts:
  #     get:
  #       $ref: ../swagger/paths/contacts/get.yaml #додаємо ще один шлях
  /contacts:
    post:
      $ref: ../swagger/paths/contacts/post.yaml #додаємо ще один шлях
  /contacts/{id}:
    get:
      $ref: ../swagger/paths/contacts/{id}/get.yaml
components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
